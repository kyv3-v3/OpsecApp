# OpsecApp (Android)

Android app that shows/installs apps from trusted sources.

## Build

Requirements:
- JDK 17
- Android SDK (`platforms;android-35`, `build-tools;35.0.0`)

Command:

```bash
./gradlew :app:assembleDebug
```

Generated APK:

`app/build/outputs/apk/debug/app-debug.apk`
